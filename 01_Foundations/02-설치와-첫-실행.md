# 설치와 첫 실행

## 목표

- `install`, `status`, `send` 흐름을 이해한다.
- clone-local install과 daemon endpoint 개념을 연결한다.

## 먼저 읽을 원본

- `clawhip/README.md`
- `clawhip/install.sh`
- `clawhip/src/lifecycle.rs`
- `clawhip/src/main.rs`

## 권장 학습 순서

1. `README`의 install pitch 확인
2. `Commands::Install`, `Commands::Status`, `Commands::Send`가 어디서 처리되는지 확인
3. daemon endpoint(`[REDACTED_PHONE]:25294`)와 client 호출 구조 이해

## 체크포인트

- 왜 `status`는 daemon health 확인인가?
- `send`와 `emit`의 차이는 무엇인가?
- clone-local 설치 흐름이 일반 CLI 설치와 어떻게 다른가?

## 같이 보면 좋은 예제

- `examples/config/basic-discord.toml`
- `examples/config/tmux-omx-session.toml`

처음에는 `basic-discord.toml`만 읽고, tmux/OMX 연동이 필요해질 때 `tmux-omx-session.toml`로 넘어가면 됩니다.
